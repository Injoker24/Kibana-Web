---
applications:
  - name: APPNAME
    memory: 256M
    disk_quota: 256M
    instance: 1
    path: build
    buildpack: staticfile_buildpack
    routes:
      - route: APPHOST.APPDOMAIN
    services:
      - dynatrace-omni
    env:
      DT_CLUSTER_ID: sakuku-cobrand-web
