export enum QRISType {
    Static = 'static',
    Dynamic = 'dynamic'
}

export enum QRISTipType {
    Mandatory = 'mandatory',
    Optional = 'optional'
}

export enum QRISTipAmountType {
    Absolute = 'absolute',
    Percentage = 'percentage'
}